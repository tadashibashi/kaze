if (CMAKE_SYSTEM_NAME STREQUAL "Darwin")
    set(KAZE_PLATFORM_MACOS 1)
else()
    set(KAZE_PLATFORM_MACOS 0)
endif()

if (CMAKE_SYSTEM_NAME STREQUAL "Linux")
    set(KAZE_PLATFORM_LINUX 1)
else()
    set(KAZE_PLATFORM_LINUX 0)
endif()

if (CMAKE_SYSTEM_NAME STREQUAL "Windows")
    set(KAZE_PLATFORM_WINDOWS 1)
else()
    set(KAZE_PLATFORM_WINDOWS 0)
endif()

if (KAZE_PLATFORM_MACOS OR KAZE_PLATFORM_LINUX OR KAZE_PLATFORM_WINDOWS)
    set(KAZE_PLATFORM_DESKTOP 1)
else()
    set(KAZE_PLATFORM_DESKTOP 0)
endif()

if (CMAKE_SYSTEM_NAME STREQUAL "iOS")
    set(KAZE_PLATFORM_IOS 1)
else()
    set(KAZE_PLATFORM_IOS 0)
endif()

if (CMAKE_SYSTEM_NAME STREQUAL "tvOS")
    set(KAZE_PLATFORM_TVOS 1)
else()
    set(KAZE_PLATFORM_TVOS 0)
endif()

if (CMAKE_SYSTEM_NAME STREQUAL "visionOS")
    set(KAZE_PLATFORM_VISIONOS 1)
else()
    set(KAZE_PLATFORM_VISIONOS 0)
endif()

if (CMAKE_SYSTEM_NAME STREQUAL "watchOS")
    set(KAZE_PLATFORM_WATCHOS 1)
else()
    set(KAZE_PLATFORM_WATCHOS 0)
endif()

if (KAZE_PLATFORM_IOS OR KAZE_PLATFORM_TVOS OR KAZE_PLATFORM_WATCHOS OR KAZE_PLATFORM_VISIONOS)
    set(KAZE_PLATFORM_APPLE_DEVICE 1)
else()
    set(KAZE_PLATFORM_APPLE_DEVICE 0)
endif()

if (KAZE_PLATFORM_APPLE_DEVICE OR KAZE_PLATFORM_MACOS)
    set(KAZE_PLATFORM_APPLE 1)
else()
    set(KAZE_PLATFORM_APPLE 0)
endif()

if (CMAKE_SYSTEM_NAME STREQUAL "Android")
    set(KAZE_PLATFORM_ANDROID 1)
else()
    set(KAZE_PLATFORM_ANDROID 0)
endif()

if (KAZE_PLATFORM_IOS OR KAZE_PLATFORM_ANDROID)
    set(KAZE_PLATFORM_MOBILE 1)
else()
    set(KAZE_PLATFORM_MOBILE 0)
endif()

if (CMAKE_SYSTEM_NAME STREQUAL "Emscripten")
    set(KAZE_PLATFORM_EMSCRIPTEN 1)
else()
    set(KAZE_PLATFORM_EMSCRIPTEN 0)
endif()


